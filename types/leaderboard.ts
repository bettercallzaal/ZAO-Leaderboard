export interface LeaderboardEntry {
  rank: number;
  name: string;
  address: string;
  ogRespect: number;
  zorRespect: number;
  totalRespect: number;
}

export interface AirtableRecord {
  name: string;
  address: string;
}
